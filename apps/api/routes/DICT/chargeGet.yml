/api/v1/charge/{id}:
  get:
    tags:
      - charge
    summary: Get one charge
    parameters:
      - name: id
        in: path        
    responses:
      '200':
        description: The charge retrieve using the given ID